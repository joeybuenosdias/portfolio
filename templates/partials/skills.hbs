<section class="carousel-section">
    <h2 class="carousel-title">Skills</h2>
    <div class="carousel-content">
        {{#each skills}}
            <div class="carousel-card">
                <img
                    class="card-logo"
                    src={{this.vector}}
                />
                <p class="card-label">{{this.label}}</p>
            </div>
        {{/each}}
    </div>
</section>